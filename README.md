# AI-Resume-Checker
Built an AI Resume Analyzer that parses resumes, computes ATS compatibility scores (0–100), identifies keyword gaps against job descriptions, and generates AI-rewritten summaries — using Claude API, Chart.js radar visualization, and vanilla JavaScript
